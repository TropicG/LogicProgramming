∃ ∀ ¬

Задача 1 

permutation([H | T], Result) :- permutation(T,Q), insert(H,Q,Result)
Подава се опашката да бъде пермутирана и се връща в Q, след това добавяме в Q нашето H за да получим Result 
Не забравяй че insert ще добави H на място което не винаги е задължително да бъде първо!!!

Задача 2 

lessOrEqual(X,Y) :- X =< Y.

isАscSorted([]). 
isAscSorted([_]).
isAscSorted([X, Y | T]) :- isSorted(X,Y), isSorted([Y | T]).

Задача 3 

Окей тази задача я виж, падала се е на контролни хиляда пъти

Направи следните предикати в Пролог, като X е списък от числа, а У е списък от списъци от числа
1) p1(X,Y) <--> Има елемент на X, който е елемент на Y 
2) p2(X,Y) <--> Има елемент на Х, който е във всеки елемент на Y
3) p3(X,Y) <--> Всеки елемент на X е в елемент на Y
4) p4(X,Y) <--> Всеки елемент на X е във всеки елемент на Y

Как бига изглеждали формулите за да са верни:
1) ∃a∃b member(A,B)
2) ∃a∀b member(A,B) <--> ∃a¬∃b¬member(A,B) (¬∃b е същото като ∀b¬) (нарочно е сложен втори negate след b за да може после като се прехвърли negate да стане нормално)
(Малко нарочно правят такива формули, защото пролог по добре работи с формули който използват само and операциите)
3) ∀a∃b member(A,B) <--> ¬Ea¬∃b member(A,B)
4) ∀a∀b member(a,b) <---> ¬Ea¬¬∃b¬member(a,b) <-->  ¬Ea∃b¬member(a,b)

Решения:
1) member(A,X), member(B,Y), member(A,B) (не забравяй X е единствен елемент, а Y е списък от списъци,затова X трябва да присъства в Y
2) member(A,B), not((member(B,Y),not(member(A,B)))) (онзи израз който го изведохме отгоре това му е еквивалента в пролог)
3) not((member(A,X),not((member(B,Y), member(A,B)))))
4) not(member(A,X), member(B,Y), not(member(A,B)))

Задача 4

prefix(P,L) :- append(P, _, L). // съществува някакво _ което като се append-Не с P се получава L, тоест P e префикс
sufix(S,L) :- append(_, S, L). // съществува някакво _ което е като се append-не с S се получава L, тоест S е суфикс

Задача 5

Задачата е да напишеш предикат който проверява дали даден sublist се намира в list

infix(L, SubL) :- suffix(S, L), prefix(SubL,S) 
Идеята тук е да се намери suffix-a на листа L, след това да се провери дали sublist-а който търсим е префикс на суфикса на L

infix(L, SubL) :- prefix(P,L), sufix(SubL, P)
Същата логика като горното просто вместо да започнем със суфикса, започваме със префикса 

Задача 6
Да се намери подредица в масива

subsequence([], _).
subsequence([X | Q], [X | T]) :- subsequence(Q, T).
subsequence([X | Q], [H | T]) :- subsequence([X | Q], T), X \= H